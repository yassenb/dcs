<!DOCTYPE project [<!ENTITY common SYSTEM "../common.xml">]>
    
<project name="client" default="redist" basedir=".">
    &common;

    <target name="redist" depends="init">
        <ant dir="${src_dir}/dcs/client" target="redist"/>
    </target>
</project>
