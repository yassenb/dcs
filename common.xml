<property name="base_dir" location="."/>
<property name="src_dir" location="./src"/>

<target name="init">
    <mkdir dir="build"/>
    <mkdir dir="redist/lib"/>
</target>

<target name="clean">
    <delete dir="build"/>
    <delete dir="redist"/>
</target>
